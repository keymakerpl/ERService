<UserControl x:Class="ERService.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ERService.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             Height="350" Width="600"
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             Dialog:DialogParticipation.Register="{Binding}">
    <Grid >

        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderThickness="1"
                BorderBrush="LightGray"
                CornerRadius="3"
                Background="GhostWhite">
            <Border.Effect>
                <DropShadowEffect/>
            </Border.Effect>
            <StackPanel Margin="10 20 10 15">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <Label Content="Login:" Margin="5"/>
                    <TextBox Grid.Column="1" Width="200" Margin="5"
                             Text="{Binding Login, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="1" Content="Hasło:" Margin="5"/>
                    <PasswordBox x:Name="PasswordBox" Margin="5"
                                 Grid.Column="1" Grid.Row="1" MinWidth="150"/>

                    <Button Grid.Row="2" Grid.Column="1" Content="Zaloguj" Margin="15 10 15 0"
                            Width="80" HorizontalAlignment="Right"
                            Command="{Binding LoginCommand}"
                            CommandParameter="{Binding ElementName=PasswordBox}"/>

                </Grid>
            </StackPanel>
        </Border>
        
    </Grid>
</UserControl>
