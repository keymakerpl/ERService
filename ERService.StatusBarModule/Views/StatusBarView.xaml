<UserControl x:Class="ERService.StatusBar.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ERService.StatusBar.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >

    <Border Grid.Row="1" x:Name="ListBorder" Margin="10 0 10 10"
            BorderThickness="1" BorderBrush="#FF83919F">
        <Grid >
            <TextBlock Text="{Binding Message}"
                   Margin="10 0 10 0"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Foreground="Black"/>
        </Grid>
    </Border>
</UserControl>
