<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ERService.Settings.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:mah="http://metro.mahapps.com/winfx/xaml/shared" x:Class="ERService.Settings.Views.SettingsView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="650"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Dialog:DialogParticipation.Register="{Binding}">

    <UserControl.Resources>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Header" Value="{Binding DataContext.Title}"/>
        </Style>
    </UserControl.Resources>

    <Grid>

        <TabControl prism:RegionManager.RegionName="SettingsTabControlRegion" Margin="8 10 8 5"/>

    </Grid>
</UserControl>
